# 部署说明 · 叶纸的投资报告

这是一个**纯静态网站**：没有后端、所有依赖（React / 字体）都已下载进仓库，**国内可直接访问，不依赖任何外网 CDN**。
GitHub Pages 从 `main` 分支根目录部署，线上地址：https://leafpaper.github.io/Inves-Report/

> 2026-08 改版（Quiet Ledger）：移除浏览器端 Babel（-3.1MB）、新增明暗双主题（与报告详情页共享 `ca-theme` 存储键）、
> 卡片直读 v8 判断链字段（行动档位 / 质地 / 估值标签 / 披露日）、SEO 元数据与 sitemap。

---

## 一、本地预览（不用装任何东西）

直接**双击 `index.html`** 用浏览器打开即可。`file://` 下线上数据 fetch 会失败，页面自动回退到
`reports.data.js` 的快照并显示「离线快照」标记；持仓面板离线时显示占位说明（真实持仓只在线上展示）。

---

## 二、改前端代码后要重新构建

首页应用源码在 **`src/app.jsx`**（JSX）。改完跑一次：

```
sh build.sh        # Git Bash / macOS / Linux
build.cmd          # Windows 双击或 cmd
```

会用 esbuild 把它编译成根目录的 `app.js`（页面实际加载的文件）。**只改 `app.js` 不改 `src/app.jsx` 的话，下次构建会被覆盖。**
需要 Node.js（任何近年版本，走 `npx esbuild`,首次联网下载）。

其余文件（`_ds_bundle.js` / `holdings.js` / `tokens/*.css` / `index.html`）改完即生效，无需构建。
改了 JS 文件记得把 `index.html` 里对应 `?v=N` 版本号 +1,避免 Pages 缓存出旧文件。

---

## 三、数据从哪来（不要手改）

| 数据 | 来源 |
|---|---|
| `data/reports.json` | `claude-company-analysis` 仓库的 `scripts.update_index` 发布报告时 upsert |
| `reports.data.js` | 同上脚本自动刷新（`window.REPORTS_RAW` 离线兜底快照） |
| `data/holdings.json` `data/holdings_history.json` | GitHub Action `sync-holdings.yml` 每日从长桥同步 |
| `reports/{slug}/` | 每篇报告的成品 HTML + card-metadata.json（发布时拷入） |
| `sitemap.xml` | 新增报告后可重新生成（按 reports.json 列出全部报告页） |

---

## 四、主题

- 自动跟随系统明暗；导航栏 ☾/☀ 按钮可显式切换,选择存 `localStorage['ca-theme']`,
  与 `reports/{slug}/` 报告详情页共用一份记忆（切页不割裂）。
- 颜色全部走 `tokens/colors.css` 的 CSS 变量；暗色在该文件的两个暗色块里改（两块内容需保持一致）。

---

## 五、目录速览

| 路径 | 作用 |
|---|---|
| `index.html` | 首页骨架 + 布局样式（GitHub Pages 入口） |
| `src/app.jsx` → `app.js` | 首页应用逻辑（源码 → esbuild 构建产物） |
| `build.sh` / `build.cmd` | 一键构建 |
| `_ds_bundle.js` | 设计系统组件（Mascot / Tabs / SearchInput 等） |
| `holdings.js` | 增强版持仓表（HoldingsTablePro） |
| `tokens/` | 设计 token（颜色/字体/间距/效果,含暗色主题）与自托管字体 |
| `vendor/` | 自托管 React（不依赖 unpkg） |
| `assets/` | og-image / 图标 / 吉祥物图 |
| `sitemap.xml` `robots.txt` | 搜索引擎收录 |

历史遗留（未被主页引用,留着无害）：`styles.css`（@import 壳）、`assets/js/render.js`、`assets/meme*.jpg`、`vendor/babel.min.js` 已删除。

---

## 六、发布流程（这台机器）

PowerShell 没有 git,**用 Git Bash**；首次先:
`git config --global --add safe.directory F:/vscode/_tools/Inves-Report`
HTTPS push 凭据在 Windows 凭据管理器。commit + push 到 `main` 即上线（Pages 1-2 分钟生效）。

> ⚠️ 投资有风险，入市需谨慎，本站仅供参考不构成投资建议。
